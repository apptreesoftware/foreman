import type { Instance } from "../instance.ts";

/** Scaffolds a repository's `.foreman/` and its project board. Implemented in Task 7. */
export async function runInit(_instance: Instance, _out: (line: string) => void): Promise<number> {
  throw new Error("not implemented until Task 7");
}
